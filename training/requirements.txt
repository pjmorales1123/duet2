# Separate Python 3.12 environment; never install into the simulator's .venv.
lerobot[training]==0.6.1
torch==2.8.0
torchvision==0.23.0
torchcodec==0.7.0
numpy==2.2.6
mujoco==3.12.0
PyOpenGL==3.1.10
# Integrated learned simulator UI; does not change the training NumPy contract.
fastapi==0.141.1
uvicorn==0.52.4
